# Test artifacts
node_modules/
playwright-report/
test-results/
blob-report/
*.trace.zip

# Don't ignore recorded fixtures (they're committed for replay mode)
# But ignore any local-only overrides
fixtures/local_*.json
